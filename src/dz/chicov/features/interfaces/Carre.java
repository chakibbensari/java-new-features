package dz.chicov.features.interfaces;

// With this annotation (optional), we cannot declare more then one abstract method
@FunctionalInterface
public interface Carre {
	
	Double getPermietre(Double x);

}
